function Navbar() {
  return (
    <div>
      <nav className="Navbar">
        <div className="links">
          <a href="/">Home</a>
          <a href="/work">Work</a>
          <a href="/contact">Contact</a>
        </div>
      </nav>
    </div>
  );
}

export default Navbar;
